# Supabase (optional). If set, uploaded CSV data is saved and restored on refresh.
# Get these from your project: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Mapbox (optional). If set, account detail shows a location map when locations have lat/lng.
# Get a public token at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token
